<li class="reg_card">
    <span class="question"><%= reg_card.question %></span>
    <span class="answer">
      Answer: <%= reg_card.answer %>
      <% if current_user.admin? || current_user.id == @reg_card.user_id %>
			<%= link_to "delete", reg_card, method: :delete, data: { confirm: "You sure?",class: "delete"}, title: reg_card.question,class: "delete"%>
	  <%end%>
    </span>
</li>